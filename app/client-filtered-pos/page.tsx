// This page can be renamed if it's no longer about filtering,
// e.g., app/all-pos-list/page.tsx
// For now, keeping the existing route for simplicity of the example.
import PoDetailsClient from "@/components/po-details-client"

export default function ClientPOsListPage() {
  // Renamed function for clarity
  return <PoDetailsClient />
}
